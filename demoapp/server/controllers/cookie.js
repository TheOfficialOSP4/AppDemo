const cookieController = {};

cookieController.createCookie = (req, res, next) => {
  return next();
};

cookieController.updateCookie = (req, res, next) => {
  return next();
};

cookieController.deleteCookie = (req, res, next) => {
  return next();
};

module.exports = cookieController;
